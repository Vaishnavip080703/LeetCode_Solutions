class Solution(object):
    def intersect(self, nums1, nums2):
        d={}
        l = []
        for i in range(len(nums1)):
            if nums1[i] in nums2:
                print(nums1[i])
                nums2.remove(nums1[i])
                l.append(nums1[i])
        #print(l)
        return l

nums1 = [4,9,5]
nums2 = [9,4,9,8,4]
v = Solution()
print(v.intersect(nums1,nums2))
