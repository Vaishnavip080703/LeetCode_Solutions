class Solution(object):
    def searchMatrix(self, n,t):
        for i,j in enumerate(n):
            if t in j:
                found = 1
                break
            else:
                found = 0
        if found:
            return True
        else:
            return False
                
matrix = [[1,3,5,7],[10,11,16,20],[23,30,34,60]]
target = 3
v = Solution()
print(v.searchMatrix(matrix,target))
        
            
