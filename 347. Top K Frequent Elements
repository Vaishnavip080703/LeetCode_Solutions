class Solution(object):
    def topKFrequent(self, nums, k):
        d={}
        for i in nums:
            if i not in d:
                d[i] = 1
            else:
                d[i] +=1
        a = []
        e = sorted(d.items(), key=lambda x: x[1], reverse=True)
        for i in range(k):
            a.append(e[i][0])
            print(e[i][0])
        print(e,a)
        return a
        
        
        
nums = [4,1,-1,2,-1,2,3]
k = 2
v = Solution()
v.topKFrequent(nums,k)
