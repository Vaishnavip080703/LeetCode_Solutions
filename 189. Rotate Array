class Solution(object):
    def rotate(self, nums, k):
        n = len(nums)
        k = k % n

        nums[:] = nums[-k:] + nums[:-k]

        return nums


nums = [1,2,3,4,5,6,7]
k = 3
v = Solution()
print(v.rotate(nums, k))
